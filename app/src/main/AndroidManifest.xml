<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".NotificationThrillerApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.NotificationThriller">
        
        <!-- Welcome/Tutorial Activity - Launcher entry point -->
        <activity
            android:name=".ui.WelcomeActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- First Contact Activity - The Hook -->
        <activity
            android:name=".ui.FirstContactActivity"
            android:exported="false"
            android:screenOrientation="portrait">
        </activity>
        
        <!-- Splash Screen Activity -->
        <activity
            android:name=".ui.SplashScreenActivity"
            android:exported="false"
            android:theme="@style/Theme.SplashScreen"
            android:screenOrientation="portrait">
        </activity>
        
        <!-- Main Game Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false">
        </activity>
        
        <!-- Archive Activity -->
        <activity
            android:name=".ui.ArchiveActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity">
        </activity>
        
    </application>

</manifest>
